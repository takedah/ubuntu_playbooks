---
# Ubuntu 26.04 の apt には tmux 3.6a が入っているのでソースビルドはしない。
- name: Install tmux
  become: true
  ansible.builtin.apt:
    name: tmux
    state: present
    update_cache: true
    cache_valid_time: 3600
